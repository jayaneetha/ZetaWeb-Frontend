{
  "name": "zeta-web",
  "version": "0.0.1",
  "description": "Frontend for ZetaWeb",
  "keywords": "",
  "license": "",
  "author": "coder.clix@gmail.com",
  "type": "module",
  "scripts": {
    "prebuild": "npx rimraf build",
    "build": "parcel build --public-url ./ ./src/index.html --out-dir build",
    "dev": "parcel ./src/index.html --out-dir build --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.50.0"
  },
  "dependencies": {
    "@amcharts/amcharts5": "5.1.12",
    "@popperjs/core": "2.11.5",
    "bootstrap": "5.1.3",
    "jquery": "3.3.1"
  }
}
